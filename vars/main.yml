# --- Global ---
product_name: "textreview"

# --- Frontend (Next.js) ---
frontend_name: "textreview-web"
frontend_image_name: "ghcr.io/text-review/text-review"
frontend_image: "{{ frontend_image_name }}:{{ image_tag | default('latest') }}"
frontend_port: 3000

# --- Backend (ASP.NET Core) ---
backend_name: "textreview-backend"
backend_image_name: "ghcr.io/text-review/text-review-backend"
backend_image: "{{ backend_image_name }}:{{ image_tag | default('latest') }}"
backend_port: 8080